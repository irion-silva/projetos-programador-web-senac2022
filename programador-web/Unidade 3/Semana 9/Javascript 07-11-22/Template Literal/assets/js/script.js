const instituicao = `Senac`;
const curso = `Programação Web`;
const unidade1 = 5;
const unidade2 = 7;
const unidade3 = 12;

const encapsulado = `Instituição: ${instituicao}
Curso: ${curso}
Carga Horária:
Unidade 1: ${unidade1} horas
Unidade 2: ${unidade2} horas
Unidade 3: ${unidade3} horas
Total de horas: ${unidade1 + unidade2 + unidade3} horas`;

console.log(encapsulado);